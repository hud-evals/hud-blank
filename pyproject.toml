[project]
name = "hud-blank"
version = "0.1.0"
description = "A minimal HUD v6 environment: a text-reasoning task and a tool-using task."
requires-python = ">=3.11"
dependencies = [
    "hud-python>=0.6.0",
]

[dependency-groups]
dev = [
    "pytest>=8",
]

[tool.uv]
package = false
